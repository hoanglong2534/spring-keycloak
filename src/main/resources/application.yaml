spring:
  application:
    name: keycloak

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/demo-keycloak
  datasource:
    username: keycloak
    url: jdbc:postgresql://localhost:5432/keycloak
    password: keycloak@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


server:
  port: 8999

idp:
  url: http://localhost:8180
  realm: demo-keycloak
  client-id: demo-spring-keycloak
  client-secret: sph38LLsDkL3a1s6d25LeMzjI5JGmMkY
